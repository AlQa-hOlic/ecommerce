import Header from ".";
// import { useValue, useSelect } from "react-cosmos/fixture";

export default function Fixture() {
  //   const [count, setCount] = useValue<number>("count", { defaultValue: 0 });
  //   const [option, setOption] = useSelect("option", {
  //     options: ["a", "b", "c", "d"],
  //     defaultValue: "a",
  //   });
  return <Header />;
}
